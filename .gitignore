# Secrets and local-only config
.env.local
*.local

# ntfy runtime state
ntfy/cache/
ntfy/lib/

# Bridge runtime (captured replies / logs may contain task data)
bridge/inbox/
bridge/rejected/
bridge/pending/
bridge/*.log

# Voice test artifacts
samples/*.wav

# Python
__pycache__/
*.pyc

# Python venv
.venv/

# Postgres/NATS local data + per-stack secret env (Steps 3-4)
postgres/pgdata/
postgres/.env
nats/data/

# ntfy per-host base url
ntfy/.env

# Ed25519 private keys
keys/

backups/

assets/voices/
