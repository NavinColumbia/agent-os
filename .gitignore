# Secrets and local-only config
.env.local
*.local

# ntfy runtime state
ntfy/cache/
ntfy/lib/

# Bridge runtime (captured replies / logs may contain task data)
bridge/inbox/
bridge/rejected/
bridge/pending/
bridge/processed/
bridge/*.log

# Voice test artifacts
samples/*.wav

# Python
__pycache__/
*.pyc
*.egg-info/

# Python venv
.venv/

# Postgres/NATS local data + per-stack secret env (Steps 3-4)
postgres/pgdata/
postgres/.env
nats/data/

# ntfy per-host base url
ntfy/.env

# Ed25519 private keys
keys/

backups/

assets/voices/

# Runtime-generated incident RCAs
docs/incidents/
logs/
.runtime/

# Tool-managed dependency state and local databases
.terraform/
*.sqlite3

# Security-test sentinel and generated report source
AOS_PWNED
report-source.md
