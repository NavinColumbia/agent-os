"""Agent OS v2 package.

New runtime code lives behind explicit domain and application boundaries.  The
legacy ``scripts`` tree remains available only while the strangler migration is
in progress.
"""
